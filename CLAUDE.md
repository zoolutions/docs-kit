# docs-kit — Claude Code entry point

@AGENTS.md

`AGENTS.md` is the single project-instruction file, shared with Cursor,
Copilot, Aider and Codex. Claude-only material lives in `.claude/`
(`rules/` auto-load; commands, skills) — see `.claude/README.md`.
